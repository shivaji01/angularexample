import { TodaymockDirective } from './todaymock.directive';

describe('TodaymockDirective', () => {
  it('should create an instance', () => {
    // const directive = new TodaymockDirective();
    // expect(directive).toBeTruthy();
  });
});
