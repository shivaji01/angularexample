import { HightlighttextDirective } from './hightlighttext.directive';

describe('HightlighttextDirective', () => {
  it('should create an instance', () => {
    // const directive = new HightlighttextDirective();
    // expect(directive).toBeTruthy();
  });
});
